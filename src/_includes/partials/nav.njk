<nav id="navbar" class="fixed w-full z-50 transition-all duration-500 ease-out bg-white/95 backdrop-blur-md text-black shadow-sm py-4">
    <div class="container mx-auto px-6 flex justify-between items-center">
        <a href="{{ ('/' + lang + '/') | url }}" class="cursor-pointer z-50 hover:opacity-80 transition-opacity w-32 md:w-48">
            <img src="{{ '/assets/img/HOPE_Logo_A_Black_3x.avif' | url }}" alt="Hope Has No Borders" id="nav-logo" class="w-full h-auto logo-normal logo-target object-contain" />
        </a>

        <div class="hidden lg:flex items-center gap-8 font-medium text-xs tracking-widest">
            <a href="{{ ('/' + lang + '/about/') | url }}" class="nav-link uppercase hover:text-orange-600 transition-colors relative group">{{ i18n[lang].nav_about }}</a>
            <a href="{{ ('/' + lang + '/programs/') | url }}" class="nav-link uppercase hover:text-orange-600 transition-colors relative group">{{ i18n[lang].nav_programs }}</a>
            <a href="{{ ('/' + lang + '/resources/') | url }}" class="nav-link uppercase hover:text-orange-600 transition-colors relative group">{{ i18n[lang].nav_resources }}</a>
            <a href="{{ ('/' + lang + '/blog/') | url }}" class="nav-link uppercase hover:text-orange-600 transition-colors relative group">{{ i18n[lang].nav_blog }}</a>
            <a href="{{ ('/' + lang + '/volunteer/') | url }}" class="nav-link uppercase hover:text-orange-600 transition-colors relative group">{{ i18n[lang].nav_volunteer }}</a>
            
            <!-- Language Switcher -->
            <a href="{{ i18n[lang].lang_switch_url }}{{ page.url | replace('/en/', '') | replace('/es/', '') }}" 
               class="flex items-center gap-1 text-yarrow hover:text-black transition-colors"
               aria-label="Switch to {{ i18n[lang].lang_switch_label }}">
               <i data-lucide="globe" class="w-3 h-3"></i>
               {{ i18n[lang].lang_switch_label }}
            </a>

            <div id="nav-divider" class="h-4 w-px bg-neutral-300"></div>

            <a href="https://www.paypal.com/donate/?hosted_button_id=JG5GT99FFDQ4L" target="_blank" class="bg-yarrow text-black px-6 py-3 rounded-sm font-bold uppercase hover:bg-black hover:text-white transition-all duration-300 shadow-lg shadow-yarrow/20 hover:shadow-xl">
                {{ i18n[lang].nav_donate }}
            </a>
        </div>

        <button class="lg:hidden z-50" onclick="toggleMobileMenu()">
            <i data-lucide="menu" id="mobile-menu-icon" class="text-black"></i>
        </button>
    </div>
</nav>

<div id="mobile-menu" class="fixed inset-0 bg-white text-black z-40 flex flex-col items-center justify-center gap-8 hidden opacity-0 transition-opacity duration-300">
    <a href="{{ ('/' + lang + '/about/') | url }}" class="text-3xl font-serif font-light capitalize hover:text-orange-600 transition-colors">{{ i18n[lang].nav_about }}</a>
    <a href="{{ ('/' + lang + '/programs/') | url }}" class="text-3xl font-serif font-light capitalize hover:text-orange-600 transition-colors">{{ i18n[lang].nav_programs }}</a>
    <a href="{{ ('/' + lang + '/resources/') | url }}" class="text-3xl font-serif font-light capitalize hover:text-orange-600 transition-colors">{{ i18n[lang].nav_resources }}</a>
    <a href="{{ ('/' + lang + '/blog/') | url }}" class="text-3xl font-serif font-light capitalize hover:text-orange-600 transition-colors">{{ i18n[lang].nav_blog }}</a>
    <a href="{{ ('/' + lang + '/volunteer/') | url }}" class="text-3xl font-serif font-light capitalize hover:text-orange-600 transition-colors">{{ i18n[lang].nav_volunteer }}</a>
    
    <a href="{{ i18n[lang].lang_switch_url }}{{ page.url | replace('/en/', '') | replace('/es/', '') }}" class="text-lg font-bold uppercase tracking-widest text-neutral-500 hover:text-yarrow transition-colors">
        Switch to {{ i18n[lang].lang_switch_label }}
    </a>

    <a href="https://www.paypal.com/donate/?hosted_button_id=JG5GT99FFDQ4L" target="_blank" class="text-3xl font-serif font-light capitalize hover:text-yarrow transition-colors text-yarrow font-bold">{{ i18n[lang].nav_donate }}</a>
</div>
